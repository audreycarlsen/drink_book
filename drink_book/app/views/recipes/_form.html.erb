<%= form_for(@recipe) do |f| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :drankbook_id %><br>
    <%= f.text_field :drankbook_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :process %><br>
    <%= f.text_area :process %>
  </div>
  <div class="field">
    <%= f.label :ingredients %><br>
    <%= f.select(:ingredients, @ingredients, {}, {:multiple => true}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
