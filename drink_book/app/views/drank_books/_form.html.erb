<%= form_for(@drank_book) do |f| %>
  <% if @drank_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drank_book.errors.count, "error") %> prohibited this drank_book from being saved:</h2>

      <ul>
      <% @drank_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :recipes %><br>
    <%= f.select(:recipes, @recipes, {}, {:multiple => true}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
